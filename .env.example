# Backend API URL
# Defaults to http://localhost:8000 if not set
EXPO_PUBLIC_BACKEND_URL=http://localhost:8000

# App Mode
# Options: "onboarding" or "regular"
# - onboarding: Shows only the chat screen (no tabs)
# - regular: Shows full app with workout, history, and chat tabs
# Defaults to "regular" if not set
EXPO_PUBLIC_APP_MODE=regular
